import React from 'react';
import { Calendar } from './Calendar';

export const Dashboard: React.FC = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            <Calendar />
        </div>
    )
};
